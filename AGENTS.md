# desk 仓库开发规范

本文件供在本仓库工作的开发 Agent 与贡献者使用，适用于整个仓库。用户明确的任务授权优先；不要把本规范解释为要求每一步重新确认。

## 1. 项目范围与文档职责

desk 是一体化中学辅助学习 Agent，本期覆盖高中语文、数学、英语的教—学—练—评—析—改闭环。

开始工作时，阅读相关文档并检查工作区状态：

- [README](README.md)：产品范围、角色权限、技术架构、数据与接口约束、A1–A8 核心验收。
- [阶段目标](docs/stage-goals.md)：M0–M4 的依赖、功能范围、交付物及 T01–T07 补充验收；历史 PR 编号是需求索引，不是 GitHub Pull Request。
- [工程 Review 规范](docs/review-guidelines.md)：代码、架构、AI 与文档改动的评审方法。
- [需求评审记录](docs/requirements-review.md)：原文一致性追踪和历史订正，不替代工程 Review。
- [原文留档](docs/reference/original-product.md)：历史来源。正文和配图中的旧名称不应直接覆盖当前产品决策；变更留档必须说明原因并同步来源校验信息。

当前仓库只有需求及规范文档，尚无应用源码、依赖清单、数据库迁移、构建脚本或 CI。不得把规划中的路径、接口、命令和阶段标记为已经实现。文档改动不需要创建应用骨架来满足检查。

本期题目难度不随作答自动变化；学生画像可动态更新。PaddleOCR 实现与定时家长周报可选，但拍照上传/人工校正及家校基础阶段报告仍必需。原文五周周期是建议，不是完成承诺。调整产品范围或技术选型时同步需求与阶段目标，不能只在开发规范中改变它们。

## 2. 模块归属与目录演进

按实际功能需要建立目录，不预先生成空模块或照搬已删除旧项目。下表是首次实现时的建议布局，**目前均未创建**；若采用其他布局，在首个实现改动中同步本文和运行说明。

| 建议路径 | 责任 |
| --- | --- |
| `apps/web` | TypeScript + React 网页，覆盖教师、学生及精简管理/家长页面 |
| `apps/mobile` | uni-app 小程序；共享业务契约，不复制服务端权限与判分规则 |
| `apps/api` | Go + Gin + GORM 对外 API、领域服务、迁移、asynq Worker 与业务集成 |
| `services/ai` | Python + FastAPI 内网服务，解析、检索、模型推理与建议输出 |
| `contracts` | 版本化 OpenAPI 及 Go↔Python 契约，描述请求、响应、错误与事件语义 |
| `deployments` | 演示/SaaS/私有部署配置及运维资源 |

代码、测试、迁移和运维辅助脚本放在负责该行为的模块。后端初期采用模块化单体和独立 AI 服务，不默认拆为微服务。共享抽象应有真实使用需求；至少两个消费者需要同一稳定抽象时再考虑提取共享包。

生成客户端或类型文件不得手改；修改契约来源后按已提交工具配置重新生成，并检查实际消费者。不要为了遵循建议目录而引入尚未确定的框架、包管理器或发布平台。

## 3. 架构与契约

- 网页与小程序通过 Go API 访问业务；前端不得直连 MySQL、Redis、向量库或 Python AI 服务。MinIO 预签名直传是例外，凭据签发、完成校验及下载授权由 Go 负责。
- Go 负责身份、权限、状态转换、审核、试卷发布、成绩确认与事务。Gin handler 处理 HTTP 输入和认证主体，领域服务作业务决定，外部协议封装在适配器中。不要在 handler 中直接调用模型或散落业务写入。
- 每个业务对象有明确的权威写入模块；跨模块变更调用其服务，不从其他模块绕过状态和授权规则直接改表。合理的 GORM 使用不强制套没有语义的 repository 包装。
- Python FastAPI 仅提供内网解析、检索、OCR/模型能力和结构化建议；不持有权威业务写权限。DeepSeek/OpenAI 兼容调用经 Python 服务，模型供应商通过配置切换。
- Go Worker 消费 Redis/asynq 任务并调用 Python HTTP；不要假定 Python 原生消费 asynq 协议。调用设置超时、取消/失败状态和有上限的重试。
- 对外接口使用 `/api/v1`，错误遵守 README 的 `code/message/details/trace_id`，HTTP 状态与业务码含义稳定；客户端按业务码处理，不匹配错误文案。结构化错误不得泄露内部异常和学生正文。
- OpenAPI 是公开接口契约的统一来源。Go、网页、小程序及相关生成客户端需验证一致；Go↔Python 的内部 schema 也须版本化校验。新增不兼容字段或语义时说明部署顺序及兼容窗口。
- JWT + Casbin 执行角色、租户、班级、资源和工具权限。JWT 签名有效不等于当前仍有任课或家长绑定权限。身份和租户来自认证上下文，不能信任客户端或模型自报的 ID。

## 4. 数据、状态与一致性

- MySQL/InnoDB 是资源版本、审核、试卷、答卷、成绩和学习证据的事实来源。Redis 缓存、向量索引和报表是派生状态；不能用“最新缓存”或模型文本替代业务记录。
- Redis 可用于会话和队列，需有有效期、撤销、持久化/恢复配置及相应失效语义。关键业务事件由 MySQL outbox 保障，不能把持久化副作用仅交给可丢失的队列消息。
- Chroma 用于演示，Qdrant 用于放量，通过适配层解耦；索引可重建。检索先过滤租户、审核和权限，返回/发布前再次按主库确认有效版本，处理下架与权限变更的索引延迟。
- 资源实质修改产生新草稿并重审；正式组卷与发布只能使用审核通过的有效版本。已发布试卷保存题目、答案、分值、顺序快照，不能随题库编辑变化。
- 教师确认成绩时，事务写成绩版本与 outbox 事件。消费者按成绩版本幂等更新画像、报告和 SIS；重复投递不重复计分或累计证据。更正应重算并保留旧版本。
- 内部成绩确认与画像更新、SIS 同步分别显示状态；外部失败不伪装成功，也不回滚已确认成绩。超时后结果未知的操作先查询/对账，不能无条件重放副作用。
- 迁移文件及 schema 版本受 Git 管理，数据库改动说明兼容、回填与恢复方式。不要仅靠生产启动时的 GORM AutoMigrate 代替可审查迁移。SQL 参数化；必要原生 SQL 说明 GORM 无法正确表达的语义。
- 测试使用隔离、可丢弃的数据和基础设施；事务、唯一约束、锁、并发及迁移必须在真实 MySQL 上验证，Mock 不能证明数据库语义。应用迁移到真实学校或生产环境需在任务授权范围内。

## 5. AI 与教育业务边界

- 模型输出、检索文件和用户上传内容均是不可信输入，不是系统指令或授权来源。自然语言参数、工具参数、OCR、初评分数和引用必须校验 schema、资源归属及当前业务状态。
- 发布试卷、审核资源、确认/更正成绩只能由有权限的业务操作完成，不能由模型直接落权威记录。学生不能借工具或提示词调用监考与成绩写入能力。
- 正式考试中的答案披露由服务端规则控制；检索、提示上下文、工具结果、流式输出、引用原文和导出都不能绕过。不能仅靠“不泄题”提示词或前端隐藏答案。
- 伴学引用必须真实、相关、可定位且访问合法；无依据明确不足。提示词或模型变更需回归受影响学科和任务模式，不以输出更流畅代替质量证据。
- L1 原始证据、L2 摘要与 L3 画像保持版本和解释链。BKT 更新依据有效判分证据，主观反馈/AI 初评不能当作已确认考试成绩；更正和证据删除需重算关联状态。
- 自然语言改单题默认转换标签条件并检索候选；无题时返回缺口，不偷偷放宽硬约束或使用未审核题。教师手改/拍照替换仍需新版本审核。
- 家长只访问绑定孩子的必要报告，不能获得完整对话。行为风险与相似度仅为教师复核线索，不自动认定作弊。使用虚构、脱敏的学生样本进行开发和演示。

## 6. 前端与移动端

React 与 uni-app 是当前基线；路由库、状态库、样式方案和包管理器尚未确定，不把其他项目的 Next.js App Router、Server Components 或 Tailwind 专用规则当作既有要求。首次选择应说明理由并记录，后续沿用已落地约定。

- 请求集中到有类型的 API 客户端/领域服务层，组件处理交互与呈现；网页、小程序保持相同业务契约、错误和时间语义。
- 路由/页面私有组件、hooks 和测试就近放置；共享组件只承载实际复用能力。命名遵循对应语言与已有工具配置，不强制所有语言采用同一风格。
- 每个应用集中解析环境配置，只向客户端暴露允许公开的字段。模型、数据库与存储密钥不得进入构建产物。
- 会话传输方案须兼顾网页和小程序，保持 JWT + Casbin 架构。网页避免将长期凭据放入可被脚本读取的持久存储；Cookie 方案验证 CSRF/SameSite，Bearer 方案验证存储、失效与撤销。未选定前不声称已实现 HttpOnly 会话。
- 上传、解析、批改、组卷、导出显示真实进度/失败；弱网草稿与提交确认分开，未送达不能显示成功。处理权限撤销、空数据、并发编辑及过期会话。
- 公式、题组、中文材料、英语长文和答案分区应正确渲染。重要 UI 变更验证窄屏、键盘、焦点和可读性；移动端相关变更用目标小程序真机或明确记录的替代方式验证。
- 优先复用已有图标、资源和设计约定；新增素材有来源说明，截图与录屏使用虚构数据并去除令牌、预签名地址和学生个人信息。

## 7. 开发与验证命令

当前可直接使用的检查为 Git 状态与文档检查：

```bash
git status --short
git diff --check
git diff --cached --check
```

同时检查修改文档的相对链接/锚点、表格、阶段引用、需求和规范一致性。纯文档整理不需要启动数据库、安装应用依赖或编写与正文逐字重复的测试。

实现模块后，以实际提交的依赖清单、锁文件、脚本和 CI 为准。下列只是对应生态的检查方向，**不是当前可用的仓库命令**：

| 模块存在后 | 应建立的验证 |
| --- | --- |
| Go 模块及 `go.mod` | 在模块目录执行 `go test ./...`、`go vet ./...`，检查 gofmt；并发改动补充适用的 race 与基础设施测试 |
| React/uni-app 依赖及锁文件 | 用所选包管理器的已定义脚本做类型、lint、相关测试与构建；不要假设存在根级 `pnpm check` |
| Python 环境与工具配置 | 按已提交配置执行测试、lint/类型检查；不要假设存在 pytest/Ruff 或可用的裸全局 Python 依赖 |
| Compose/部署文件 | 配置校验、依赖健康检查、迁移/恢复及目标部署路径；只在隔离环境做破坏性演练 |
| AI 契约与评测集 | 结构化响应、引用、权限/泄题、失败重试和模型版本回归；注明模型、参数、数据集和测量边界 |

首个可运行模块必须补充真实的安装、启动、检查命令与环境示例，再在此处链接到对应说明。迭代时跑聚焦验证，交付前执行实际存在且与改动相关的完整检查；契约变更验证所有受影响消费者。准确报告未执行、失败或受环境限制的检查，不能以“文档通过”声称 A1–A8 产品验收通过。

## 8. 部署、安全与可观测性

支持同一代码的 SaaS 多租户及单校私有部署；Compose 是演示建议，当前没有指定托管厂商、容器文件名或自动发布流水线。不要创建虚构部署命令或声称 push 会自动上线。

- 仅暴露必要网页/Go 业务入口，Python、数据库、Redis、MinIO 管理接口和向量库默认内网；按需受控提供对象预签名访问。
- 配置学校时区、公开基地址、模型供应商、外发策略、功能开关与凭据；私有化不自动等于离线，模型外发须可禁用。
- `.env`、JWT/模型密钥、学校真实数据、完整对话及临时签名 URL 不进入 Git、日志、fixture 或评审截图；环境示例仅写变量名和安全占位符。
- 区分进程存活与依赖就绪；探针不泄露连接信息，依赖是否阻塞启动需按职责定义。使用 trace_id 串联 HTTP、AI 和异步任务。
- 监控队列积压、模型超时、成绩/索引同步失败和推荐时限；数据库与原文件纳入备份，缓存/向量可重建，恢复方案包括必要配置与密钥管理。
- 部署改动说明迁移、兼容、发布顺序与回滚；不能只回滚应用而忽略 schema、在途任务和模型/索引版本。

## 9. 分支、提交与交付

当前默认分支为 `main`，尚未建立 `dev` 集成分支、生产标签策略或发布工作流。正常协作建议从最新 `origin/main` 建短期分支，通过 PR 回到 `main`；不要照搬其他项目的 `dev → main` 流程。用户明确授权在当前分支提交推送时，按授权完成，不额外要求创建 PR。

分支可使用 `feat/api-grade-confirmation`、`fix/ai-citation-scope`、`docs/agent-guidelines`。提交及 PR 标题建议为 `<type>(<scope>): <中文摘要>`；scope 采用实际负责模块，如 `web`、`mobile`、`api`、`ai`、`contracts`、`deploy`、`docs`、`repo`。

- 提交前检查状态，只暂存当前任务内容；不覆盖他人的未提交改动。存在并行修改或归属不清时使用独立 worktree，不能自动清空工作区。
- 不强推共享分支，不删除远程历史或移动标签来绕过冲突。目标分支有新提交时审查差异、解决冲突并重新验证受影响内容。
- PR 和评审文字使用中文，标识符与日志保留原文。描述先说明问题与最终行为，再列相关责任模块、验证、未验证边界；复杂改动再补迁移/部署/回滚信息。不要为简单文档修改填满无关模板项。
- 有 PR 时可使用现有且匹配的语义标签；仓库尚无标签或 CI 规则时不假定它们存在。未完成必要实现/验证的 PR 保持 Draft；相关 CI 失败或最新提交未验证时不宣称可合并。
- 显著 UI 改动附可用的截图/录屏证据；契约改动包含源定义及生成结果，环境变更更新安全示例和运行说明。
- 评审依据 [工程 Review 规范](docs/review-guidelines.md)，记录评审提交 SHA；代码更新后原结论不自动覆盖新版本。单独的评审请求默认只读，除非用户同时授权修改、发布 GitHub review/评论或合并。
- 用户已要求修复并推送时完成相应动作和结果核对；“推送”不等于部署、合并其他 PR 或发布版本。将来引入 CI/发布流程时先提交清晰配置与操作说明，不移动或复用已发布标签。

## 10. 本次规范适配说明

本文保留所提供参考规范中的模块归属、统一契约、权威写入、真实基础设施验证、聚焦提交和明确交付证据原则，按 desk 的 Go/Python、MySQL、JWT/Casbin、MinIO 和教育业务重写。电商订单/支付、Prisma/PostgreSQL、特定前端/托管平台及强制 dev 分支流程不适用。未来实现应更新真实目录与命令，不以规范中的建议布局冒充现有代码。
