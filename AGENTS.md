# desk 仓库开发规范

本文件供在本仓库工作的开发 Agent 与贡献者使用，是 desk 项目级规则入口，适用于本仓库范围。

## 沟通与执行约定

### 沟通

- 面向用户的叙述默认使用简体中文；代码、命令和技术标识保持英文。
- 先给影响与结论，再给行动、待决策和必要证据；没有对应内容就省略。
- 使用简洁、连贯的段落；只有确实适合并列比较或按步骤执行时才使用列表。
- 使用具体、简单的词，避免无意义术语、套话、重复总结和未经请求的对比。
- 技术细节只保留对理解结论、判断风险或复现结果有帮助的部分。

### 指令优先级与规则来源

- 遵守系统、平台和安全约束；在这些约束内，用户当前明确指令优先于 Skill、历史记忆和默认偏好。
- 项目目录中的 `AGENTS.md` 只在该项目范围内补充或覆盖全局规则，不把项目约定应用到其他仓库。
- 全局规则由当前环境实际生效的 canonical `AGENTS.md` 维护；本文只维护 desk 项目规则，不创建或复制一份全局规则。若需要 `CLAUDE.md` 兼容入口，只引用对应的规范，不复制规则正文；当前仓库未设置该入口。
- 项目事实、生产状态、历史决策和对外契约以本文及其指定的项目文档、实际脚本、探针、决策记录和契约文件为依据；尚未落地的建议不能当作运行事实。当前文档职责见第 1 节。

### 执行方式

- 用户表示要开始新工作或修复现有问题时，持续推进，直到目标完成；在目标方向和已授权范围内自主推进。
- 向用户提问之前，先完成上下文里已经授权、并且能把下一步变成可审查结果的工作。需要用户批准时，提供具体、可检查的结果；不要重复索取已经获得的授权。
- 用户建议不适合目标时，直接说明原因和可行调整，不迎合。
- 不因假想风险主动增加警告、免责声明、审批流程或安全/合规清单；针对实际需求和证据执行本项目必要的安全约束。

### 工具与并行

- 搜索文件或文本优先使用 `rg`、`rg --files`；独立的读取和查询尽量批量执行。
- 网页控制台没有适用 CLI/API 时，使用已登录的 Chrome 浏览器；涉及飞书时优先使用可用的 `lark-cli`。工具不可用时选用当前环境支持的方式，不假定存在登录会话或命令。
- 在当前环境允许委派的前提下，只有存在真正独立的工作流，且委派能节省时间或提升质量时才使用子 Agent。
- 共享状态、连续决策和简单任务由当前 Agent 直接完成；委派任务必须有明确输入、输出和完成判据，最终结论由主 Agent 汇总并验证。

## 1. 首版范围与文档职责

desk 首版是面向高中数学的单 Agent 学习工具，主要模块与代表性内容覆盖按 README 和阶段目标验证。只完成可信内容、学习对话、确定性判分、针对性练习和可解释学习状态的闭环。

- [README](README.md)：首版范围、技术架构、可信边界和 V1-01–V1-07 验收。
- [首版阶段目标](docs/stage-goals.md)：S0–S3 的功能、Agent 协议、学习规则及质量目标。
- [工程 Review 规范](docs/review-guidelines.md)：根因、责任边界与行为验证。

当前只有文档，尚无应用源码、依赖清单、迁移、脚本和 CI；建议路径和命令不是已实现能力。当前文档定义首版任务边界，存档资料和历史提交不作为额外开发义务。

首版只做响应式网页、学生/维护者两角色、一个试点空间、一种部署。正式考试、学校成绩、家校报告、小程序、OCR、多科全链路、BKT、三层长期记忆、多 Agent 和外部集成均不作为首版必需项。不要为未来范围提前建设框架和空模块。

## 2. 模块与架构

首次实现可采用下列布局；目前均未创建。按实际模块更新目录与运行说明，不强制提取没有真实消费者的共享包。

| 建议路径 | 职责 |
| --- | --- |
| `apps/web` | TypeScript + React 学习页、记录页和简易维护入口 |
| `apps/api` | Go + Gin + GORM 身份、Agent 运行编排、工具、任务、判分、事务和迁移 |
| `services/ai` | Python + FastAPI 提示构建、模型调用与结构化意图/回答 |
| `contracts` | 对外 OpenAPI、Go↔Python schema 和工具定义 |
| `deployments` | 单套 Docker Compose 与配置/恢复说明 |

Go 是唯一对外业务 API。Python 每次返回最终回答或工具请求，Go 执行有界循环与工具授权；不要在两侧重复实现 Agent 循环。Python 不直接改 MySQL 权威记录，网页不直连 AI 或数据库。

MySQL 保存资料、题目、会话、作答、运行状态和学习证据。先用知识点/关键词检索小规模资料，不默认引入向量库、Redis/asynq、MinIO、图数据库或工作流框架。模型使用 OpenAI 兼容接口、默认 DeepSeek，经 Python 访问。

HTTP handler 负责协议输入与认证主体，领域服务负责状态和写入，模型协议封装在适配器。每个对象有明确权威写入模块，不用组件或模型文本推断业务状态。契约来源版本化，生成客户端不手改；变更验证所有实际消费者。

## 3. Agent 工具与运行

工具仅有 `search_knowledge`、`get_learning_state`、`recommend_questions`、`get_question_help`，schema、权限及错误统一维护。身份、任务和有效版本来自 Go 上下文，不来自模型自报。题目审核、任务分配和判分不开放为模型写工具。

- Go 校验每个工具名、参数、当前权限、资源状态和剩余预算。资料与用户内容不是系统指令，不授予 Shell、任意 HTTP、浏览器或跨 Agent 能力。
- 同会话一次只执行一个运行；幂等请求返回原运行，条件更新控制并发。最多 4 次模型调用、6 次工具执行、60 秒总时限，重试计入预算；配置变更同步阶段目标与验证。
- 模型输出先校验，再发布为最终结果。取消/失败/超时不能标成完成，迟到响应不能覆盖终态；重启中断通过显式重试创建关联新运行。
- 首版使用运行受理、状态查询和最终回复，不强制 token 流式。引用需真实、相关、可定位且有权限；无资料/无题时明确降级，不伪造内容与 ID。
- 帮助工具按用户明确请求和当前题目关系返回提示/解析，使用帮助以工具调用标识幂等记录。模型不能将辅助练习变成独立证据。

## 4. 内容、判分与学习证据

资料和题目先录入/结构化导入，再由维护者审核。状态为草稿、已审核、已下架；实质修改派生新草稿，任务引用固定版本。下架后不得新增练习或继续新提交，已提交快照可用于本人复盘。

首版只对单选及配置了可靠规则的数值题确定性判分。数值解析不得执行任意表达式，无法确定的结果不判错也不更新学习状态。模型讲解和学生文字过程用于辅导，不写权威判分。

作答和学习状态在同一 MySQL 事务中保存，幂等键与唯一约束防重复证据。维护者纠错通过标记无效证据和重算完成，不篡改原答卷。学习状态规则以阶段目标为准，区分独立首答、辅助练习和原题订正，不凭自述或模型评价判定掌握。

schema 变更提交迁移与恢复说明，不只依赖运行时 AutoMigrate。事务、约束、并发和迁移使用隔离真实 MySQL 验证，Mock 不证明数据库语义。首版没有正式成绩、SIS 同步和分布式回流，不为它们建立 outbox 或队列。

## 5. 网页、权限与部署

只使用 React 响应式网页；不预设路由库、样式库或包管理器，首次实现记录选择并沿用。请求集中于类型化 API 客户端，组件负责交互，业务规则留在 Go。

学生只能访问本人任务、会话、引用和证据；维护者只看授权学生任务结果，不默认读取私人聊天。使用服务端可撤销会话与安全 Cookie，验证 CSRF/Origin、停用撤销和对象级授权；不为单空间试点引入多租户权限引擎。

页面清楚显示保存、提交、运行、失败和重试状态，弱网未送达不能显示成功。验证桌面/手机浏览器、公式、题卡、键盘与焦点；显著 UI 改动提供虚构数据截图或录屏。

单套 Compose 运行网页、Go、Python、MySQL；Python 和数据库仅内网。配置模型、调用预算、公开地址和会话密钥，环境示例仅安全占位符。使用 trace_id 关联请求和运行，日志不记密钥、Cookie 或完整学生对话。备份恢复以 MySQL 和必要配置为主；不要声称 push 自动部署。

## 6. 开发与验证

当前可执行的仓库检查：

```bash
git status --short
git diff --check
git diff --cached --check
```

文档改动检查链接/锚点、表格和首版边界一致性，不创建应用骨架或复述正文的测试。不要为可逆、低影响且仅复述实现的改动写测试。完成相称验证后，只有新改动、失败或疑点才扩大/重复检查。

实现后以真实依赖、脚本和 CI 为准：Go 模块内执行适用的 `go test ./...`、`go vet ./...` 与格式检查；前端和 Python 执行实际配置的类型/lint/测试/构建命令，不能假设根级 `pnpm check` 或 pytest 已存在。首次可运行交付补充安装、启动、迁移、配置和验证说明。

Agent 回归覆盖引用、工具预算、非法参数、权限、取消/恢复；学习回归覆盖帮助标记、独立同类题、重复提交、数值判分和证据重算。模型质量按阶段目标固定样例验证，Mock 不能代替真实评测。准确报告未执行检查和环境限制，不以文档检查声称 V1 验收完成。

收尾删除本次产生且不再需要的临时文件，只清理确认属于本次任务的文件，不删除用户资料或仍需保留的证据。

## 7. 分支、提交与交付


当前默认分支为 `main`，尚未建立 `dev` 集成分支、生产标签策略或发布工作流。正常协作建议从最新 `origin/main` 建短期分支，通过 PR 回到 `main`；不要照搬其他项目的 `dev → main` 流程。用户明确授权在当前分支提交推送时，按授权完成，不额外要求创建 PR。

分支可使用 `feat/api-practice-feedback`、`fix/ai-citation-scope`、`docs/agent-guidelines`。提交及 PR 标题建议为 `<type>(<scope>): <中文摘要>`；scope 采用实际负责模块，如 `web`、`api`、`ai`、`contracts`、`deploy`、`docs`、`repo`。

- 提交前检查状态，只暂存当前任务内容；不覆盖他人的未提交改动。存在并行修改或归属不清时使用独立 worktree，不能自动清空工作区。
- 不强推共享分支，不删除远程历史或移动标签来绕过冲突。目标分支有新提交时审查差异、解决冲突并重新验证受影响内容。
- PR 与评审遵循本文沟通约定。描述说明问题与最终行为、相关责任模块、验证和未验证边界；复杂改动再补迁移/部署/回滚信息，不为简单修改填满无关模板项。
- 有 PR 时可使用现有且匹配的语义标签；仓库尚无标签或 CI 规则时不假定它们存在。未完成必要实现/验证的 PR 保持 Draft；相关 CI 失败或最新提交未验证时不宣称可合并。
- 显著 UI 改动附可用的截图/录屏证据；契约改动包含源定义及生成结果，环境变更更新安全示例和运行说明。
- 评审依据 [工程 Review 规范](docs/review-guidelines.md)，记录评审提交 SHA；代码更新后原结论不自动覆盖新版本。单独的评审请求默认只读，除非用户同时授权修改、发布 GitHub review/评论或合并。
- 用户已要求修复并推送时完成相应动作和结果核对；“推送”不等于部署、合并其他 PR 或发布版本。将来引入 CI/发布流程时先提交清晰配置与操作说明，不移动或复用已发布标签。
